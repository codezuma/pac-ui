export function generateMetadata(title: string, description: string) {
  return {
    title: `${title} - Pac UI`,
    description: `${description} Built with React, react-hook-form, Zod, and Tailwind CSS, part of Pac UI, a type-safe form system for React.`,
  }
}
