import ComponentCodePreview from "@/components/app/component-code-preview"
import { generateMetadata } from "../utils/metadata"
import { FormTextareaBasic } from "./form-textarea-basic"

export const metadata = generateMetadata(
  "Form Textarea",
  "A multi-line text input bound to react-hook-form, with automatic error display."
)

# Form Textarea

A multi-line text input bound to a [form](/docs/form)'s `control`, for longer free-text fields.

## Example

<ComponentCodePreview
  component={<FormTextareaBasic />}
  filePath="app/docs/form-textarea/form-textarea-basic.tsx"
  classNameComponentContainer="p-8"
/>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

<CodeBlock
  code={`npx shadcn add "https://pac.chandresh.dev/c/form-textarea.json"`}
  language="bash"
/>

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<CodeBlock filePath="components/form/form-textarea.tsx" language="tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Component API

### FormTextarea

| Prop               | Type                | Description                                 |
| :------------------ | :------------------ | :-------------------------------------------- |
| control              | `Control<T>`         | The `control` object from `useForm()`          |
| name                 | `FieldPath<T>`       | The field name, type-checked against `T`       |
| label                | string               | Field label                                     |
| description          | string               | Helper text below the textarea                 |
| Icon                 | React.ReactNode      | Leading icon                                   |
| textareaClassName    | string               | Additional CSS classes on the `<textarea>`     |
| className            | string               | Additional CSS classes on the field wrapper     |
