import ComponentCodePreview from "@/components/app/component-code-preview"
import { generateMetadata } from "../utils/metadata"
import { FormPasswordBasic } from "./form-password-basic"

export const metadata = generateMetadata(
  "Form Password",
  "A password input bound to react-hook-form, with a show/hide toggle and automatic error display."
)

# Form Password

A password input built on [Form Input](/docs/form-input)'s chrome, with a built-in show/hide toggle.

## Example

<ComponentCodePreview
  component={<FormPasswordBasic />}
  filePath="app/docs/form-password/form-password-basic.tsx"
  classNameComponentContainer="p-8"
/>

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

<CodeBlock
  code={`npx shadcn add "https://pac.chandresh.dev/c/form-password.json"`}
  language="bash"
/>

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<CodeBlock filePath="components/form/form-password.tsx" language="tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Component API

### FormPassword

| Prop           | Type                | Description                                 |
| :------------- | :------------------ | :-------------------------------------------- |
| control         | `Control<T>`         | The `control` object from `useForm()`          |
| name            | `FieldPath<T>`       | The field name, type-checked against `T`       |
| label           | string               | Field label                                    |
| description     | string               | Helper text below the input                    |
| Icon            | React.ReactNode      | Leading icon                                   |
| inputClassName  | string               | Additional CSS classes on the `<input>`        |
| className       | string               | Additional CSS classes on the field wrapper     |
